<?php
$hasPrivileges = $this->account() ? $this->account()->hasSuperPrivileges() : false;
foreach ($feeds as $feed) {
    echo $this->partial("feed", array('feed' => $feed, 'isPremium' => $isPremium, "hasPrivileges" => $hasPrivileges));
}
